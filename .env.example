BRIDGE_DOMAIN=relay.example.com
BRIDGE_SITE_ID=home
# Generate each value with: python3 -c 'import secrets; print(secrets.token_urlsafe(32))'
BRIDGE_DEVICE_TOKEN=replace-with-a-random-device-token-at-least-32-characters
BRIDGE_HA_TOKEN=replace-with-a-different-random-ha-token-at-least-32-characters
BRIDGE_ALLOW_DANGEROUS_RPC=false
